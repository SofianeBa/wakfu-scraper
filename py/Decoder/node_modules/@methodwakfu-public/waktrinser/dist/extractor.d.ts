import { Token } from 'wink-tokenizer';
import { IComparison } from './models/Comparison';
export default class Extractor {
    /**
     * Extract condition elements from token
     * @param token
     * @param params
     */
    static condition(token: Token, params: number[]): string;
    /**
     * Extract ternary result from localized token
     * @param {Tokenizer.Token} token
     * @returns {string}
     */
    static ternaryResult(token: Token): string;
    /**
     * Extract ternary comparison symbol and value from localized token
     * @param {Tokenizer.Token} token
     * @returns {IComparison}
     */
    static ternaryComparison(token: Token): IComparison;
}
